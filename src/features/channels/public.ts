/** Controller that owns the mounted channel screen lifecycle and transport synchronization. */
export { ChannelScreenController, type ChannelScreenControllerOptions } from "./application/channel-screen-controller";
/** Cross-leaf navigation from durable channel identity metadata to an exact agent feed. */
export { routeAgentReference, type AgentFeedNavigationTarget } from "./application/agent-feed-router";
/** Native Obsidian channel directory and group-chat presentation component. */
export { ChannelsView, type ChannelsViewOptions } from "./ui/channels-view";
