/** Slash-command catalogue and deterministic submission policy. */
export { SlashCommandController } from "./application/slash-command-controller";

/** Host capability contract required by the slash-command controller. */
export type { SlashCommandControllerOptions } from "./application/slash-command-controller";
