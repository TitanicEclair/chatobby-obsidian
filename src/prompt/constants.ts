/** Lines of context to include before the cursor in the excerpt. */
export const CONTEXT_EXCERPT_BEFORE = 12;

/** Lines of context to include after the cursor in the excerpt. */
export const CONTEXT_EXCERPT_AFTER = 6;

export const CONTEXT_EXCERPT_MAX_CHARS = 6_000;
export const CONTEXT_SELECTION_MAX_CHARS = 12_000;
export const CONTEXT_MAX_HEADINGS = 64;
export const CONTEXT_HEADING_MAX_CHARS = 200;
export const CONTEXT_MAX_OPEN_NOTES = 12;
export const CONTEXT_MAX_IMAGES = 5;
